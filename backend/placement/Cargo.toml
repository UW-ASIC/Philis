[package]
name = "pnr-placement"
version = "0.1.0"
edition = "2021"

[dependencies]
pnr-engine = { path = "../engine" }
pnr-cells = { path = "../cells" }
pnr-constraints = { path = "../constraints" }

[dev-dependencies]
pnr-cells = { path = "../cells", features = ["test-fixtures"] }
