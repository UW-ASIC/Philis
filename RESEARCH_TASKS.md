See @docs/books/\*, these have LOTS of analog layout theory :)
These get applied to frontend/constraints/ Which directly feeds to the backend/engine/ where it is configured/wired up using traits to fit in the beautiful abstract placement and routing system :).

Needs to be in a workflow where handoffs are minimizzed, instead each agent writes their own markdown that are compiled by 1 final agent because handoffs lose information, when they write markdown with everything, we minimize loss of information.

Consider the feedback loop too!

=> Follow up is docs/constraints/ being filled, and docs/constraints/USAGE.md that explains how the constraints are threaded in the backend/engine/ and at which stage (placement or routing)
