[package]
name = "dp"
version = "0.1.0"
edition = "2021"

[dependencies]
pnr_core = { path = "../../kernel/core" }
analog = { path = "../../kernel/analog" }
# dp reuses gp's shared SA mechanics (RNG, objective/legality wiring, canvas).
gp = { path = "../gp" }
